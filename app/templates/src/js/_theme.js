/**
 *
 * Theme javascript
 */

(function( $ ) {
	"use strict";

	/**
	 * Initially hide Orbit
	 */
	$(document).ready(function () {
	  // orbit handler
	  $('ul.slider-items').removeClass('hide');
	});

}(jQuery));